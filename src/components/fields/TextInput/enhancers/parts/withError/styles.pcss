@import 'styles/constants.pcss';

.container {
  position: relative;
}

.error {
  color: rgba(220, 0, 0, 0.8);
  padding: 5px 0;
  font-family: var(--baseFontFamily);
  font-style: italic;
  font-size: 13px;
  line-height: 15px;
  min-height: 25px;
}
