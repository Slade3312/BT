@import "styles/constants.pcss";

.container {
  display: flex;
  position: relative;
  padding: 0 20px;
}

.component {
  width: 100%;
  max-width: var(--contentLength);
  margin: 0 auto;
  flex: 1 0 auto;
}
