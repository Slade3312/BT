@import "styles/constants.pcss";

.hoverable {
  box-shadow: inset 0 0 0 3px transparent;
  transition: box-shadow 0.2s ease-out;
}

.hoverable:hover {
  cursor: pointer;
  box-shadow: inset 0 0 0 3px var(--inProgressGraphicColor);
  transition: none;
}
