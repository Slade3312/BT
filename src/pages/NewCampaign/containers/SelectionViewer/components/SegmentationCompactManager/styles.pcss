@import "styles/constants.pcss";

.emptyRow {
  display: block;
  padding: 30px 0 10px 0;
  text-align: center;
  font: var(--mainFont);
  font-size: 15px;
  border-top: 1px solid rgba(170, 170, 170, 0.45);
}
