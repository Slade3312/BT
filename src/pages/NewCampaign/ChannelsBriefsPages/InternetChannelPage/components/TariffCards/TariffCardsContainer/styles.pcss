@import "styles/constants.pcss";

.component {
  position: relative;
}

.loader {
  position: absolute;
  width: 100%;
  height: 100%;
  background: rgba(240, 240, 240, 0.6);
}

.wrapper {
  font-family: var(--mainFontFamily);
  margin-bottom: 15px;
}
