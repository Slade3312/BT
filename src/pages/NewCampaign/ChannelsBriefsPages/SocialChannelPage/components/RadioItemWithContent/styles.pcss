.component {
  display: flex;
  padding: 20px 20px 10px 30px;
  border: 2px solid rgba(40, 40, 40, 0.2);
  min-height: 100px;

  &.selected {
    border-color: #f7c441;
  }

  &.last {
    margin-right: 0;
  }
}

.wrapper {
  padding-right: 15px;
}
